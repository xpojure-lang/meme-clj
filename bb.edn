{:paths ["src" "test"]
 :tasks {beme {:doc "beme CLI: run, repl, to, from, version"
              :task (do (require '[beme.alpha.runtime.cli])
                        (apply (resolve 'beme.alpha.runtime.cli/-main) *command-line-args*))}
         test-beme {:doc "Run all .beme tests (eval + fixture)"
                   :task (do (require '[beme.alpha.test-runner])
                             (let [fails ((resolve 'beme.alpha.test-runner/run-all-beme-tests))]
                               (System/exit (if (zero? fails) 0 1))))}
         test-cljs {:doc "Run ClojureScript tests under Node.js"
                    :task (try
                            (shell "clojure" "-M:cljs-test")
                            (finally
                              (babashka.fs/delete-tree "cljs-test-runner-out")))}
         test-all {:doc "Run all test suites (Babashka + JVM + ClojureScript)"
                   :task (do (shell "bb" "test-beme")
                             (println)
                             (shell "clojure" "-X:test")
                             (println)
                             (run 'test-cljs))}}}
