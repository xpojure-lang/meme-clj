; demo.pfx — uses prelude utilities from the prefix language.

println("=== prefix: traced functions ===")

def(add trace("add" fn([a b] +(a b))))
def(double trace("double" fn([x] *(2 x))))

double(add(10 11))
